# NODE_ENV specifies the environment in which the application is running.
# Common values: 'development', 'production', or 'test'.
NODE_ENV=development

# PORT defines the port on which the server will run.
# Example: 3000, 4000, etc.
PORT=4000

# MONGO_URI is the connection string for your MongoDB database.
# Replace with your own MongoDB connection string.
# Example: mongodb://localhost:27017/your-database
MONGO_URI=your-mongo-uri-here

# JWT_SECRET is the secret key used to sign and verify JSON Web Tokens (JWTs).
# Replace with a strong secret key for your application.
# Example: a complex string like 'your_jwt_secret_key_123!'
JWT_SECRET=your-secret-key-here
